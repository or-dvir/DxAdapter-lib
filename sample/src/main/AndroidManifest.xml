<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.hotmail.or_dvir.dxadapter">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.ActivityHorizontalRv">
        </activity>
        <activity android:name=".activities.ActivityExpandableFilterable"/>
        <activity android:name=".activities.ActivityFilter"/>
        <activity
            android:name=".activities.ActivityStickyHeader"
            android:label="Sticky Header"/>
        <activity
            android:name=".activities.ActivityMultiType"
            android:label="Multi Type"/>
        <activity
            android:name=".activities.ActivityInnerViews"
            android:label="Inner Views"/>
        <activity android:name=".activities.ActivityMain">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>